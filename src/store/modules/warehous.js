const state = {
    warehouseId: ''
};
const mutations = {
    setWarehouseId(state, {id}) {
        state.warehouseId = id
    }
};
const actions = {};

export default {
    state,
    mutations,
    actions
}
